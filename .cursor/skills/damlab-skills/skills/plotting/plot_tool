#!/usr/bin/env bash
# Self-locating wrapper: finds the skill's conda python and plot_tool.py
# relative to this script's own location, regardless of install path.
SKILL_DIR="$(dirname "$(readlink -f "$0")")"
exec "$SKILL_DIR/bin/python" "$SKILL_DIR/plot_tool.py" "$@"
